# PeopleCounter 유닛 3 — /dev 마운트만 (create_host_path: false)
services:
  backend:
    volumes:
      - type: bind
        source: /dev/bushub-people-counter-3
        target: /dev/bushub-people-counter-3
        bind:
          create_host_path: false
