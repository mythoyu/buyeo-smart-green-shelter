# LG 냉난방기 직접 제어 Modbus 패킷 테스트 가이드

## 개요

이 문서는 LG 냉난방기와의 Modbus RTU 직접 통신에서 사용되는 실제 패킷을 정리한 것입니다. Serial Tool로 직접 테스트 및 디버깅할 수 있도록 16진수 패킷 형식으로 제공합니다.

> **참고**: 이 문서는 실내기 중앙제어 주소 N=0을 기준으로 합니다. 다른 주소를 사용하는 경우 주소 계산 공식을 참고하세요.

## 📦 사용 제품: PMBUSC00A (Modbus Converter)

이 문서는 **PMBUSC00A (Modbus Converter)**를 사용한 LG 냉난방기 직접 제어를 기준으로 작성되었습니다.

### PMBUSC00A 제품 정보

| 항목                 | 내용                         |
| -------------------- | ---------------------------- |
| **제품명**           | PMBUSC00A (Modbus Converter) |
| **연동 제품**        | Multi / Single 모델          |
| **장착 타입**        | 실외기 내부 장착             |
| **최대 연결 수량**   | 실내기 최대 9대              |
| **실내기 주소 범위** | 00~08 (N=0~8)                |
| **주의사항**         | PI485 중복 사용 불가         |
| **통신 프로토콜**    | Modbus RTU                   |

### 실내기 주소 설정

PMBUSC00A는 최대 9대의 실내기를 연결할 수 있으며, 각 실내기는 중앙제어 주소(N)로 구분됩니다:

- **실내기 주소 00**: N=0 (이 문서의 기본 예시)
- **실내기 주소 01**: N=1
- **실내기 주소 02**: N=2
- ...
- **실내기 주소 08**: N=8

> **중요**: 실내기 주소는 PMBUSC00A 매뉴얼에 따라 설정해야 합니다. 주소 설정이 잘못되면 통신이 불가능합니다.

## Modbus RTU 프레임 구조

```
[Slave ID] [Function Code] [Data] [CRC-16]
   1 Byte     1 Byte      N Bytes   2 Bytes
```

**CRC-16 계산:**

- 다항식: 0x8005 (x^16 + x^15 + x^2 + 1)
- 초기값: 0xFFFF
- XOR 출력: 0x0000
- 입력/출력 반전: Yes

## 📌 Modbus 주소 체계

### 주소 계산 공식

LG 냉난방기는 실내기 중앙제어 주소(N)를 기반으로 레지스터 주소를 계산합니다:

- **Coil/Discrete Register**: `Register = N × 16 + ①`
- **Holding/Input Register**: `Register = N × 20 + ①`

**예시 (실내기 주소 0, N=0인 경우):**

- Coil Bit 1 (전원): Register = 0 × 16 + 1 = **1**
- Holding 40001 (운전 모드): Register = 0 × 20 + 1 = **1**
- Holding 40002 (바람 세기): Register = 0 × 20 + 2 = **2**
- Holding 40003 (설정 온도): Register = 0 × 20 + 3 = **3**
- Input 30001 (Error 코드): Register = 0 × 20 + 1 = **1**
- Input 30002 (실내 온도): Register = 0 × 20 + 2 = **2**

**예시 (실내기 주소 1, N=1인 경우):**

- Coil Bit 1 (전원): Register = 1 × 16 + 1 = **17** (0x0011)
- Holding 40001 (운전 모드): Register = 1 × 20 + 1 = **21** (0x0015)
- Holding 40002 (바람 세기): Register = 1 × 20 + 2 = **22** (0x0016)
- Holding 40003 (설정 온도): Register = 1 × 20 + 3 = **23** (0x0017)
- Input 30001 (Error 코드): Register = 1 × 20 + 1 = **21** (0x0015)
- Input 30002 (실내 온도): Register = 1 × 20 + 2 = **22** (0x0016)

**예시 (실내기 주소 2, N=2인 경우):**

- Coil Bit 1 (전원): Register = 2 × 16 + 1 = **33** (0x0021)
- Holding 40001 (운전 모드): Register = 2 × 20 + 1 = **41** (0x0029)
- Holding 40002 (바람 세기): Register = 2 × 20 + 2 = **42** (0x002A)
- Holding 40003 (설정 온도): Register = 2 × 20 + 3 = **43** (0x002B)
- Input 30001 (Error 코드): Register = 2 × 20 + 1 = **41** (0x0029)
- Input 30002 (실내 온도): Register = 2 × 20 + 2 = **42** (0x002A)

> **PMBUSC00A 제한사항**: 최대 9대의 실내기만 연결 가능 (주소 00~08). 주소 09 이상은 사용할 수 없습니다.

### 주소 매핑 테이블 (N=0 기준)

| 기능           | 타입              | Function Code | 주소 | 설명                                                    |
| -------------- | ----------------- | ------------- | ---- | ------------------------------------------------------- |
| **전원 제어**  | Coil (Read/Write) | 0x01 (Read)   | 1    | 전원 상태 읽기                                          |
|                |                   | 0x05 (Write)  | 1    | 전원 ON/OFF 설정                                        |
| **운전 모드**  | Holding Register  | 0x03 (Read)   | 1    | 운전 모드 읽기                                          |
|                |                   | 0x06 (Write)  | 1    | 운전 모드 설정 (0:냉방, 1:제습, 2:송풍, 3:자동, 4:난방) |
| **바람 세기**  | Holding Register  | 0x03 (Read)   | 2    | 바람 세기 읽기                                          |
|                |                   | 0x06 (Write)  | 2    | 바람 세기 설정 (1:약, 2:중, 3:강, 4:자동)               |
| **설정 온도**  | Holding Register  | 0x03 (Read)   | 3    | 설정 온도 읽기                                          |
|                |                   | 0x06 (Write)  | 3    | 설정 온도 설정 (16.0~30.0℃, ×10)                        |
| **실내 온도**  | Input Register    | 0x04 (Read)   | 2    | 실내 온도 읽기 (-99.0~99.0℃, ×10)                       |
| **Error 코드** | Input Register    | 0x04 (Read)   | 1    | Error 코드 읽기 (0-255)                                 |

## 통신 설정

| 항목                    | 값          |
| ----------------------- | ----------- |
| 프로토콜                | Modbus RTU  |
| 속도 (Baud Rate)        | 9600 bps    |
| 데이터 비트 (Data Bits) | 8 bit       |
| 정지 비트 (Stop Bits)   | 1 stop bit  |
| 패리티 (Parity)         | None Parity |
| Slave ID                | 1 (기본값)  |

> **주의**: Slave ID는 실제 장비 설정에 따라 다를 수 있습니다. 기본값은 1입니다.

## 📋 빠른 참조: 완성된 패킷 표 (CRC 계산 완료)

테스트 툴에서 복사-붙여넣기하여 바로 사용할 수 있는 완성된 패킷 목록입니다.

### 읽기 명령어 (Read Commands)

| 명령어       | 기능            | 완성된 패킷 (16진수)      | 설명                                                         |
| ------------ | --------------- | ------------------------- | ------------------------------------------------------------ |
| GET_POWER    | 전원 상태 읽기  | `01 01 00 01 00 01 9A 9C` | 전원 ON/OFF 상태 확인                                        |
| GET_MODE     | 운전 모드 읽기  | `01 03 00 01 00 01 84 0A` | 현재 운전 모드 확인 (0:냉방, 1:제습, 2:송풍, 3:자동, 4:난방) |
| GET_SPEED    | 바람 세기 읽기  | `01 03 00 02 00 01 25 CA` | 현재 바람 세기 확인 (1:약, 2:중, 3:강, 4:자동)               |
| GET_TEMP     | 설정 온도 읽기  | `01 03 00 03 00 01 74 0A` | 현재 설정 온도 확인 (×10)                                    |
| GET_CUR_TEMP | 실내 온도 읽기  | `01 04 00 02 00 01 CA 31` | 현재 실내 온도 확인 (×10)                                    |
| GET_ALARM    | Error 코드 읽기 | `01 04 00 01 00 01 84 0A` | 에러 코드 확인 (0:정상)                                      |

### 쓰기 명령어 (Write Commands)

| 명령어    | 기능       | 값  | 완성된 패킷 (16진수)      | 설명           |
| --------- | ---------- | --- | ------------------------- | -------------- |
| SET_POWER | 전원 ON    | -   | `01 05 00 01 FF 00 9A 9C` | 전원 켜기      |
| SET_POWER | 전원 OFF   | -   | `01 05 00 01 00 00 CD CA` | 전원 끄기      |
| SET_MODE  | 냉방 모드  | 0   | `01 06 00 01 00 00 9A 9A` | 냉방 모드 설정 |
| SET_MODE  | 난방 모드  | 4   | `01 06 00 01 00 04 9B 5A` | 난방 모드 설정 |
| SET_MODE  | 자동 모드  | 3   | `01 06 00 01 00 03 DA 4A` | 자동 모드 설정 |
| SET_SPEED | 강풍 설정  | 3   | `01 06 00 02 00 03 64 4A` | 강풍으로 설정  |
| SET_SPEED | 자동 바람  | 4   | `01 06 00 02 00 04 A5 5A` | 자동 바람 설정 |
| SET_TEMP  | 25.0℃ 설정 | 250 | `01 06 00 03 00 FA 39 5A` | 25.0℃로 설정   |
| SET_TEMP  | 23.0℃ 설정 | 230 | `01 06 00 03 00 E6 7A 4A` | 23.0℃로 설정   |
| SET_TEMP  | 18.0℃ 설정 | 180 | `01 06 00 03 00 B4 3B 5A` | 18.0℃로 설정   |

> **사용 방법**: 위 패킷을 Serial Tool에 복사-붙여넣기하여 바로 전송할 수 있습니다. 공백 없이 연속으로 입력하거나, 공백을 제거한 후 사용하세요.

## 1. 전원 제어 패킷

### 1.1 전원 상태 읽기 (GET_POWER)

**요청 패킷 (Function Code 0x01 - Read Coils):**

```
Slave ID: 0x01
Function Code: 0x01
Start Address: 0x0001 (High Byte, Low Byte)
Quantity: 0x0001 (High Byte, Low Byte)
CRC-16: [계산 필요]
```

**16진수 패킷:**

```
01 01 00 01 00 01 [CRC-16]
```

**CRC-16 계산:**

- 데이터: `01 01 00 01 00 01`
- CRC-16: `0x9C 0x9A` (Little Endian: `9A 9C`)

**완성된 요청 패킷:**

```
01 01 00 01 00 01 9A 9C
```

**응답 패킷 예시 (전원 ON):**

```
01 01 01 01 51 88
```

**응답 패킷 해석:**

- `01`: Slave ID
- `01`: Function Code (Read Coils)
- `01`: 바이트 수 (1바이트)
- `01`: Coil 값 (Bit 0 = 1, 전원 ON)
- `51 88`: CRC-16

**응답 패킷 예시 (전원 OFF):**

```
01 01 01 00 D0 48
```

### 1.2 전원 설정 (SET_POWER)

#### 전원 ON 설정

**요청 패킷 (Function Code 0x05 - Write Single Coil):**

```
Slave ID: 0x01
Function Code: 0x05
Coil Address: 0x0001 (High Byte, Low Byte)
Coil Value: 0xFF00 (ON)
CRC-16: [계산 필요]
```

**16진수 패킷:**

```
01 05 00 01 FF 00 [CRC-16]
```

**CRC-16 계산:**

- 데이터: `01 05 00 01 FF 00`
- CRC-16: `0x9C 0x9A` (Little Endian: `9A 9C`)

**완성된 요청 패킷:**

```
01 05 00 01 FF 00 9A 9C
```

**응답 패킷 (성공 시):**

```
01 05 00 01 FF 00 9A 9C
```

(요청과 동일한 패킷이 응답으로 반환됨)

#### 전원 OFF 설정

**요청 패킷:**

```
01 05 00 01 00 00 CD CA
```

**응답 패킷 (성공 시):**

```
01 05 00 01 00 00 CD CA
```

## 2. 운전 모드 패킷

### 2.1 운전 모드 읽기 (GET_MODE)

**요청 패킷 (Function Code 0x03 - Read Holding Registers):**

```
Slave ID: 0x01
Function Code: 0x03
Start Address: 0x0001 (High Byte, Low Byte)
Quantity: 0x0001 (High Byte, Low Byte)
CRC-16: [계산 필요]
```

**16진수 패킷:**

```
01 03 00 01 00 01 [CRC-16]
```

**CRC-16 계산:**

- 데이터: `01 03 00 01 00 01`
- CRC-16: `0x0A 0x84` (Little Endian: `84 0A`)

**완성된 요청 패킷:**

```
01 03 00 01 00 01 84 0A
```

**응답 패킷 예시 (냉방 모드, 값=0):**

```
01 03 02 00 00 B8 44
```

**응답 패킷 해석:**

- `01`: Slave ID
- `03`: Function Code (Read Holding Registers)
- `02`: 바이트 수 (2바이트 = 1개 레지스터 × 2바이트)
- `00 00`: 레지스터 값 = 0 (냉방 모드)
- `B8 44`: CRC-16

**운전 모드 값:**

- `0`: 냉방
- `1`: 제습
- `2`: 송풍
- `3`: 자동
- `4`: 난방

### 2.2 운전 모드 설정 (SET_MODE)

#### 냉방 모드 설정 (값=0)

**요청 패킷 (Function Code 0x06 - Write Single Register):**

```
Slave ID: 0x01
Function Code: 0x06
Register Address: 0x0001 (High Byte, Low Byte)
Register Value: 0x0000 (High Byte, Low Byte)
CRC-16: [계산 필요]
```

**16진수 패킷:**

```
01 06 00 01 00 00 [CRC-16]
```

**CRC-16 계산:**

- 데이터: `01 06 00 01 00 00`
- CRC-16: `0x9A 0x9A` (Little Endian: `9A 9A`)

**완성된 요청 패킷:**

```
01 06 00 01 00 00 9A 9A
```

**응답 패킷 (성공 시):**

```
01 06 00 01 00 00 9A 9A
```

#### 난방 모드 설정 (값=4)

**요청 패킷:**

```
01 06 00 01 00 04 9B 5A
```

**응답 패킷 (성공 시):**

```
01 06 00 01 00 04 9B 5A
```

#### 자동 모드 설정 (값=3)

**요청 패킷:**

```
01 06 00 01 00 03 DA 4A
```

**응답 패킷 (성공 시):**

```
01 06 00 01 00 03 DA 4A
```

## 3. 바람 세기 패킷

### 3.1 바람 세기 읽기 (GET_SPEED)

**요청 패킷 (Function Code 0x03 - Read Holding Registers):**

```
01 03 00 02 00 01 25 CA
```

**응답 패킷 예시 (중간 세기, 값=2):**

```
01 03 02 00 02 79 84
```

**응답 패킷 해석:**

- `01`: Slave ID
- `03`: Function Code (Read Holding Registers)
- `02`: 바이트 수 (2바이트)
- `00 02`: 레지스터 값 = 2 (중간 세기)
- `79 84`: CRC-16

**바람 세기 값:**

- `1`: 약
- `2`: 중
- `3`: 강
- `4`: 자동

### 3.2 바람 세기 설정 (SET_SPEED)

#### 강풍 설정 (값=3)

**요청 패킷 (Function Code 0x06 - Write Single Register):**

```
01 06 00 02 00 03 64 4A
```

**응답 패킷 (성공 시):**

```
01 06 00 02 00 03 64 4A
```

#### 자동 바람 설정 (값=4)

**요청 패킷:**

```
01 06 00 02 00 04 A5 5A
```

**응답 패킷 (성공 시):**

```
01 06 00 02 00 04 A5 5A
```

## 4. 설정 온도 패킷

### 4.1 설정 온도 읽기 (GET_TEMP)

**요청 패킷 (Function Code 0x03 - Read Holding Registers):**

```
01 03 00 03 00 01 74 0A
```

**응답 패킷 예시 (25.0℃, 값=250):**

```
01 03 02 00 FA 38 45
```

**응답 패킷 해석:**

- `01`: Slave ID
- `03`: Function Code (Read Holding Registers)
- `02`: 바이트 수 (2바이트)
- `00 FA`: 레지스터 값 = 250 (25.0℃ × 10)
- `38 45`: CRC-16

**온도 계산:**

- 레지스터 값 = 실제 온도 × 10
- 예: `250` = 25.0℃, `230` = 23.0℃

### 4.2 설정 온도 설정 (SET_TEMP)

#### 25.0℃ 설정 (값=250)

**요청 패킷 (Function Code 0x06 - Write Single Register):**

```
01 06 00 03 00 FA 39 5A
```

**응답 패킷 (성공 시):**

```
01 06 00 03 00 FA 39 5A
```

#### 23.0℃ 설정 (값=230)

**요청 패킷:**

```
01 06 00 03 00 E6 7A 4A
```

**응답 패킷 (성공 시):**

```
01 06 00 03 00 E6 7A 4A
```

#### 18.0℃ 설정 (값=180)

**요청 패킷:**

```
01 06 00 03 00 B4 3B 5A
```

**응답 패킷 (성공 시):**

```
01 06 00 03 00 B4 3B 5A
```

## 5. 실내 온도 읽기 패킷 (GET_CUR_TEMP)

**요청 패킷 (Function Code 0x04 - Read Input Registers):**

```
Slave ID: 0x01
Function Code: 0x04
Start Address: 0x0002 (High Byte, Low Byte)
Quantity: 0x0001 (High Byte, Low Byte)
CRC-16: [계산 필요]
```

**16진수 패킷:**

```
01 04 00 02 00 01 [CRC-16]
```

**CRC-16 계산:**

- 데이터: `01 04 00 02 00 01`
- CRC-16: `0x31 0xCA` (Little Endian: `CA 31`)

**완성된 요청 패킷:**

```
01 04 00 02 00 01 CA 31
```

**응답 패킷 예시 (23.5℃, 값=235):**

```
01 04 02 00 EB 38 45
```

**응답 패킷 해석:**

- `01`: Slave ID
- `04`: Function Code (Read Input Registers)
- `02`: 바이트 수 (2바이트)
- `00 EB`: 레지스터 값 = 235 (23.5℃ × 10)
- `38 45`: CRC-16

**온도 계산:**

- 레지스터 값 = 실제 온도 × 10
- 예: `235` = 23.5℃, `250` = 25.0℃
- 범위: -99.0℃ ~ 99.0℃ (값: -990 ~ 990)

## 6. Error 코드 읽기 패킷 (GET_ALARM)

**요청 패킷 (Function Code 0x04 - Read Input Registers):**

```
Slave ID: 0x01
Function Code: 0x04
Start Address: 0x0001 (High Byte, Low Byte)
Quantity: 0x0001 (High Byte, Low Byte)
CRC-16: [계산 필요]
```

**16진수 패킷:**

```
01 04 00 01 00 01 [CRC-16]
```

**CRC-16 계산:**

- 데이터: `01 04 00 01 00 01`
- CRC-16: `0x0A 0x84` (Little Endian: `84 0A`)

**완성된 요청 패킷:**

```
01 04 00 01 00 01 84 0A
```

**응답 패킷 예시 (정상, 값=0):**

```
01 04 02 00 00 B8 44
```

**응답 패킷 해석:**

- `01`: Slave ID
- `04`: Function Code (Read Input Registers)
- `02`: 바이트 수 (2바이트)
- `00 00`: 레지스터 값 = 0 (정상, 에러 없음)
- `B8 44`: CRC-16

**Error 코드:**

- `0`: 정상 (에러 없음)
- `1-255`: 에러 코드 (제품 Error code 참조)

## 7. 테스트 시나리오

### 7.1 기본 연결 테스트

**Slave ID 확인 (임의 레지스터 읽기):**

```
요청: 01 03 00 01 00 01 84 0A
응답: 01 03 02 00 00 B8 44 (정상 응답)
```

### 7.2 전원 제어 테스트 시나리오

1. **전원 상태 확인**

   ```
   요청: 01 01 00 01 00 01 9A 9C
   응답: 01 01 01 00 D0 48 (전원 OFF)
   ```

2. **전원 ON 설정**

   ```
   요청: 01 05 00 01 FF 00 9A 9C
   응답: 01 05 00 01 FF 00 9A 9C (설정 성공)
   ```

3. **전원 상태 재확인**
   ```
   요청: 01 01 00 01 00 01 9A 9C
   응답: 01 01 01 01 51 88 (전원 ON)
   ```

### 7.3 운전 모드 테스트 시나리오

1. **현재 운전 모드 확인**

   ```
   요청: 01 03 00 01 00 01 84 0A
   응답: 01 03 02 00 00 B8 44 (냉방 모드, 값=0)
   ```

2. **난방 모드로 변경**

   ```
   요청: 01 06 00 01 00 04 9B 5A
   응답: 01 06 00 01 00 04 9B 5A (설정 성공)
   ```

3. **운전 모드 재확인**
   ```
   요청: 01 03 00 01 00 01 84 0A
   응답: 01 03 02 00 04 79 84 (난방 모드, 값=4)
   ```

### 7.4 온도 설정 테스트 시나리오

1. **현재 설정 온도 확인**

   ```
   요청: 01 03 00 03 00 01 74 0A
   응답: 01 03 02 00 FA 38 45 (25.0℃, 값=250)
   ```

2. **23.0℃로 설정**

   ```
   요청: 01 06 00 03 00 E6 7A 4A
   응답: 01 06 00 03 00 E6 7A 4A (설정 성공)
   ```

3. **설정 온도 재확인**
   ```
   요청: 01 03 00 03 00 01 74 0A
   응답: 01 03 02 00 E6 79 84 (23.0℃, 값=230)
   ```

### 7.5 실내 온도 및 에러 상태 확인

1. **실내 온도 읽기**

   ```
   요청: 01 04 00 02 00 01 CA 31
   응답: 01 04 02 00 EB 38 45 (23.5℃, 값=235)
   ```

2. **Error 코드 확인**
   ```
   요청: 01 04 00 01 00 01 84 0A
   응답: 01 04 02 00 00 B8 44 (정상, 값=0)
   ```

### 7.6 전체 제어 시나리오 (냉방 모드, 25.0℃, 강풍)

1. **전원 ON**

   ```
   요청: 01 05 00 01 FF 00 9A 9C
   응답: 01 05 00 01 FF 00 9A 9C
   ```

2. **냉방 모드 설정**

   ```
   요청: 01 06 00 01 00 00 9A 9A
   응답: 01 06 00 01 00 00 9A 9A
   ```

3. **강풍 설정**

   ```
   요청: 01 06 00 02 00 03 64 4A
   응답: 01 06 00 02 00 03 64 4A
   ```

4. **25.0℃ 설정**

   ```
   요청: 01 06 00 03 00 FA 39 5A
   응답: 01 06 00 03 00 FA 39 5A
   ```

5. **상태 확인**
   ```
   전원: 01 01 00 01 00 01 9A 9C → 01 01 01 01 51 88 (ON)
   모드: 01 03 00 01 00 01 84 0A → 01 03 02 00 00 B8 44 (냉방)
   바람: 01 03 00 02 00 01 25 CA → 01 03 02 00 03 79 84 (강풍)
   온도: 01 03 00 03 00 01 74 0A → 01 03 02 00 FA 38 45 (25.0℃)
   ```

## 8. CRC-16 계산 도구

### 8.1 온라인 CRC 계산기

- [CRC-16 Modbus 계산기](https://www.lammertbies.nl/comm/info/crc-calculation.html)
- [Modbus CRC 계산기](https://www.modbustools.com/crc16.html)

### 8.2 Python CRC 계산 코드

```python
def crc16_modbus(data):
    """
    Modbus RTU CRC-16 계산
    다항식: 0x8005 (x^16 + x^15 + x^2 + 1)
    초기값: 0xFFFF
    """
    crc = 0xFFFF
    for byte in data:
        crc ^= byte
        for _ in range(8):
            if crc & 0x0001:
                crc = (crc >> 1) ^ 0xA001
            else:
                crc >>= 1
    return crc

# 사용 예시
data = [0x01, 0x05, 0x00, 0x01, 0xFF, 0x00]
crc = crc16_modbus(data)
# Little Endian 형식으로 변환
crc_bytes = [(crc & 0xFF), ((crc >> 8) & 0xFF)]
print(f"CRC-16: 0x{crc:04X} (Little Endian: {[hex(b) for b in crc_bytes]})")
```

### 8.3 JavaScript CRC 계산 코드

```javascript
function crc16Modbus(data) {
  let crc = 0xffff;
  for (let i = 0; i < data.length; i++) {
    crc ^= data[i];
    for (let j = 0; j < 8; j++) {
      if (crc & 0x0001) {
        crc = (crc >> 1) ^ 0xa001;
      } else {
        crc >>= 1;
      }
    }
  }
  return crc;
}

// 사용 예시
const data = [0x01, 0x05, 0x00, 0x01, 0xff, 0x00];
const crc = crc16Modbus(data);
// Little Endian 형식으로 변환
const crcBytes = [crc & 0xff, (crc >> 8) & 0xff];
console.log(
  `CRC-16: 0x${crc.toString(16).toUpperCase()} (Little Endian: [0x${crcBytes[0]
    .toString(16)
    .toUpperCase()}, 0x${crcBytes[1].toString(16).toUpperCase()}])`
);
```

## 9. 문제 해결

### 9.1 일반적인 오류 코드

| 오류 코드 | 설명                 | 해결 방법                   |
| --------- | -------------------- | --------------------------- |
| 0x01      | Illegal Function     | 지원하지 않는 Function Code |
| 0x02      | Illegal Data Address | 존재하지 않는 주소          |
| 0x03      | Illegal Data Value   | 잘못된 데이터 값            |
| 0x04      | Slave Device Failure | 슬레이브 장치 오류          |
| 0x06      | Slave Device Busy    | 슬레이브 장치 사용 중       |

### 9.2 디버깅 팁

1. **CRC 오류**: 패킷의 CRC-16 값을 다시 계산하여 확인
2. **응답 없음**: Slave ID, 통신 설정(baudrate, parity) 확인
3. **잘못된 응답**: Function Code와 주소 매핑 확인
4. **타임아웃**: 통신 속도와 케이블 상태 확인
5. **주소 오류**: 실내기 중앙제어 주소(N) 확인 및 주소 계산 공식 재검토

### 9.3 테스트 순서

1. **기본 연결 확인**: Slave ID 존재 여부 확인 (임의 레지스터 읽기)
2. **읽기 테스트**: 간단한 레지스터 읽기로 통신 확인 (GET_POWER, GET_MODE 등)
3. **쓰기 테스트**: 단일 레지스터 쓰기로 제어 확인 (SET_POWER, SET_MODE 등)
4. **복합 테스트**: 여러 명령어를 순차적으로 실행

### 9.4 주소 계산 오류 방지

**다른 실내기 주소 사용 시 (PMBUSC00A 기준):**

PMBUSC00A는 실내기 주소 00~08 (N=0~8)만 지원합니다.

**실내기 주소 1 (N=1) 예시:**

- Coil Bit 1 (전원): Register = 1 × 16 + 1 = **17** (0x0011)
- Holding 40001 (운전 모드): Register = 1 × 20 + 1 = **21** (0x0015)
- Holding 40002 (바람 세기): Register = 1 × 20 + 2 = **22** (0x0016)
- Input 30002 (실내 온도): Register = 1 × 20 + 2 = **22** (0x0016)

**예시 (N=1, 전원 ON):**

```
요청: 01 05 00 11 FF 00 [CRC-16]
(주소 0x0001 → 0x0011로 변경)
```

**실내기 주소 2 (N=2) 예시:**

- Coil Bit 1 (전원): Register = 2 × 16 + 1 = **33** (0x0021)
- Holding 40001 (운전 모드): Register = 2 × 20 + 1 = **41** (0x0029)

**예시 (N=2, 전원 ON):**

```
요청: 01 05 00 21 FF 00 [CRC-16]
(주소 0x0001 → 0x0021로 변경)
```

> **⚠️ PMBUSC00A 제한사항**:
>
> - 실내기 주소는 00~08 (N=0~8)만 사용 가능합니다.
> - 주소 09 이상 (N=9 이상)은 PMBUSC00A에서 지원하지 않습니다.
> - 최대 9대의 실내기만 연결 가능합니다.

## 10. 참고 자료

- [LG_HVAC_MODBUS_PROTOCOL.md](./LG_HVAC_MODBUS_PROTOCOL.md) - LG 냉난방기 Modbus 프로토콜 명세
- [HVAC_EXTERNAL_CONTROL_IMPACT_ANALYSIS.md](./HVAC_EXTERNAL_CONTROL_IMPACT_ANALYSIS.md) - 냉난방기 외부 제어 영향 분석
- [Modbus RTU 표준](https://modbus.org/docs/Modbus_over_serial_line_V1_02.pdf)

---

> 💡 **주의사항**:
>
> - 이 문서의 패킷은 테스트 목적으로만 사용하세요. 실제 운영 환경에서는 장비의 정확한 사양서를 참조하세요.
> - Slave ID는 기본값 1을 기준으로 작성되었습니다. 실제 장비 설정에 따라 변경될 수 있습니다.
> - 실내기 중앙제어 주소(N)가 0이 아닌 경우, 주소 계산 공식을 사용하여 주소를 재계산하세요.
> - **PMBUSC00A 제한사항**:
>   - 최대 9대의 실내기만 연결 가능 (주소 00~08, N=0~8)
>   - 주소 09 이상 (N=9 이상)은 사용할 수 없습니다
>   - PI485와 중복 사용 불가 (실외기 내부 장착 시)
>   - 실내기 주소는 PMBUSC00A 매뉴얼에 따라 정확히 설정해야 합니다
