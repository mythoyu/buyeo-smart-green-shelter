# PeopleCounter 유닛 2 — /dev 마운트만 (create_host_path: false)
services:
  backend:
    volumes:
      - type: bind
        source: /dev/bushub-people-counter-2
        target: /dev/bushub-people-counter-2
        bind:
          create_host_path: false
